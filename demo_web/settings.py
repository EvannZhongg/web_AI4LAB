"""
Django settings for demo_web project.

Generated by 'django-admin startproject' using Django 5.2.5.

For more information on this file, see
https://docs.djangoproject.com/en/5.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/5.2/ref/settings/
"""

from pathlib import Path
from datetime import timedelta
import os
from kombu import Queue

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media') # 文件将存储在项目根目录下的 'media' 文件夹中

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/5.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = "django-insecure-6%v%_8@88(8@1xw95_omex_=!vw99l)rp7jrw!bi79k23yc9iu"

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = [
    '70418b8712f4.ngrok-free.app', # 你的 ngrok 域名
    'localhost',
    '127.0.0.1',
]


# Application definition

INSTALLED_APPS = [
    "django.contrib.admin",
    "django.contrib.auth",
    "django.contrib.contenttypes",
    "django.contrib.sessions",
    "django.contrib.messages",
    "django.contrib.staticfiles",
    'rest_framework',
    'rest_framework_simplejwt',
    'corsheaders',
    'api',
    'pdf_parser',
]

MIDDLEWARE = [
    "django.middleware.security.SecurityMiddleware",
    "django.contrib.sessions.middleware.SessionMiddleware",
    'corsheaders.middleware.CorsMiddleware',
    "django.middleware.common.CommonMiddleware",
    "django.middleware.csrf.CsrfViewMiddleware",
    "django.contrib.auth.middleware.AuthenticationMiddleware",
    "django.contrib.messages.middleware.MessageMiddleware",
    "django.middleware.clickjacking.XFrameOptionsMiddleware",
]

ROOT_URLCONF = "demo_web.urls"

TEMPLATES = [
    {
        "BACKEND": "django.template.backends.django.DjangoTemplates",
        "DIRS": [BASE_DIR / 'templates']
        ,
        "APP_DIRS": True,
        "OPTIONS": {
            "context_processors": [
                "django.template.context_processors.request",
                "django.contrib.auth.context_processors.auth",
                "django.contrib.messages.context_processors.messages",
            ],
        },
    },
]

WSGI_APPLICATION = "demo_web.wsgi.application"


# Database
# https://docs.djangoproject.com/en/5.2/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'ai4mw_web_db',
        'USER': 'postgres',
        'PASSWORD': '15905190993zjr',
        'HOST': 'localhost',
        'PORT': '5433',  # 5432为本地postgresql
    }
}


# Password validation
# https://docs.djangoproject.com/en/5.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        "NAME": "django.contrib.auth.password_validation.UserAttributeSimilarityValidator",
    },
    {
        "NAME": "django.contrib.auth.password_validation.MinimumLengthValidator",
    },
    {
        "NAME": "django.contrib.auth.password_validation.CommonPasswordValidator",
    },
    {
        "NAME": "django.contrib.auth.password_validation.NumericPasswordValidator",
    },
]


# Internationalization
# https://docs.djangoproject.com/en/5.2/topics/i1n/

LANGUAGE_CODE = "en-us"
TIME_ZONE = "UTC"
USE_I1N = True
USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/5.2/howto/static-files/

STATIC_URL = "static/"

# Default primary key field type
# https://docs.djangoproject.com/en/5.2/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = "django.db.models.BigAutoField"


# CORS (跨域资源共享) 配置
CORS_ALLOWED_ORIGINS = [
    "https://70418b8712f4.ngrok-free.app",
    "http://localhost:5173",
    "http://localhost:5174",
    "https://web-demo-dcr.pages.dev", # 你的Cloudflare前端域名
]

# CSRF 受信来源
CSRF_TRUSTED_ORIGINS = [
    "https://70418b8712f4.ngrok-free.app",
    "https://web-demo-dcr.pages.dev/", # 你的Cloudflare前端域名
]

CORS_ALLOW_HEADERS = [
    'accept',
    'authorization',
    'content-type',
    'origin',
    'x-csrftoken',
    'x-requested-with',
    'ngrok-skip-browser-warning',
]


# REST_FRAMEWORK 配置
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': (
        'rest_framework_simplejwt.authentication.JWTAuthentication',
    ),
    'DEFAULT_PERMISSION_CLASSES': (
        'rest_framework.permissions.IsAuthenticatedOrReadOnly',
    )
}

# 默认大模型配置
DEFAULT_LLM_API_URL = "https://api.deepseek.com"
DEFAULT_LLM_API_KEY = "sk-e6412d3957f44da9b3774f7149860c15"
DEFAULT_LLM_MODEL_NAME = "deepseek-chat"

DEFAULT_VLM_API_URL = "https://dashscope.aliyuncs.com/compatible-mode/v1"
DEFAULT_VLM_API_KEY = "sk-25073b1d5af2464292d41bfb04d92a7e" 
DEFAULT_VLM_MODEL_NAME = "qwen2.5-vl-72b-instruct"

# SIMPLE_JWT 配置
SIMPLE_JWT = {
    "ACCESS_TOKEN_LIFETIME": timedelta(minutes=60),
    "REFRESH_TOKEN_LIFETIME": timedelta(days=1),
    "ROTATE_REFRESH_TOKENS": False,
    "BLACKLIST_AFTER_ROTATION": False,
    "UPDATE_LAST_LOGIN": False,

    "ALGORITHM": "HS256",
    "SIGNING_KEY": SECRET_KEY,
    "VERIFYING_KEY": "",
    "AUDIENCE": None,
    "ISSUER": None,
    "JSON_ENCODER": None,
    "JWK_URL": None,
    "LEEWAY": 0,

    "AUTH_HEADER_TYPES": ("Bearer",),
    "AUTH_HEADER_NAME": "HTTP_AUTHORIZATION",
    "USER_ID_FIELD": "id",
    "USER_ID_CLAIM": "user_id",
    "USER_AUTHENTICATION_RULE": "rest_framework_simplejwt.authentication.default_user_authentication_rule",

    "AUTH_TOKEN_CLASSES": ("rest_framework_simplejwt.tokens.AccessToken",),
    "TOKEN_TYPE_CLAIM": "token_type",
    "TOKEN_USER_CLASS": "rest_framework_simplejwt.models.TokenUser",

    "JTI_CLAIM": "jti",

    "SLIDING_TOKEN_REFRESH_EXP_CLAIM": "refresh_exp",
    "SLIDING_TOKEN_LIFETIME": timedelta(minutes=5),
    "SLIDING_TOKEN_REFRESH_LIFETIME": timedelta(days=1),

    "TOKEN_OBTAIN_SERIALIZER": "rest_framework_simplejwt.serializers.TokenObtainPairSerializer",
    "TOKEN_REFRESH_SERIALIZER": "rest_framework_simplejwt.serializers.TokenRefreshSerializer",
    "TOKEN_VERIFY_SERIALIZER": "rest_framework_simplejwt.serializers.TokenVerifySerializer",
    "TOKEN_BLACKLIST_SERIALIZER": "rest_framework_simplejwt.serializers.TokenBlacklistSerializer",
    "SLIDING_TOKEN_OBTAIN_SERIALIZER": "rest_framework_simplejwt.serializers.TokenObtainSlidingSerializer",
    "SLIDING_TOKEN_REFRESH_SERIALIZER": "rest_framework_simplejwt.serializers.TokenRefreshSlidingSerializer",
}

# --- Celery 配置 ---
# 使用 Redis 作为 Broker
# 确保 Redis 服务正在运行 (默认: localhost:6379)
CELERY_BROKER_URL = 'redis://localhost:6379/0'
CELERY_RESULT_BACKEND = 'redis://localhost:6379/0'
CELERY_ACCEPT_CONTENT = ['json']
CELERY_TASK_SERIALIZER = 'json'
CELERY_RESULT_SERIALIZER = 'json'
CELERY_TIMEZONE = TIME_ZONE # 使用 Django 的时区

CELERY_TASK_QUEUES = (
    Queue('celery', routing_key='celery'),
    Queue('stage1_text', routing_key='stage1.text'),
    Queue('stage2_vlm', routing_key='stage2.vlm'),
    Queue('stage3_chunking', routing_key='stage3.chunking'),
    Queue('stage4_extraction', routing_key='stage4.extraction'), # (抽取+融合)
    Queue('stage5_param_extraction', routing_key='stage5.param_extraction'),
    Queue('stage6_param_fusion', routing_key='stage6.param_fusion'), # (聚合+细化)
)

# --- 任务路由 (6 阶段) ---
CELERY_TASK_ROUTES = {
    'pdf_parser.tasks.task_pipeline': {'queue': 'celery'},
    'pdf_parser.tasks.run_text_analysis_task': {'queue': 'stage1_text'},
    'pdf_parser.tasks.run_vlm_analysis_task': {'queue': 'stage2_vlm'},
    'pdf_parser.tasks.run_chunking_task': {'queue': 'stage3_chunking'},
    'pdf_parser.tasks.run_model_extraction_task': {'queue': 'stage4_extraction'}, # 阶段4
    'pdf_parser.tasks.run_param_extraction_task': {'queue': 'stage5_param_extraction'}, # 阶段5
    'pdf_parser.tasks.run_param_fusion_task': {'queue': 'stage6_param_fusion'}, # 阶段6
}
# --- PDF 解析器 (pdf_parser) 配置 ---
# 是否启用 OCR (True/False)
PDF_PARSER_ENABLE_OCR = False
# 是否保存分页图片 (True/False)
PDF_PARSER_SAVE_PAGES = True
# 页面渲染DPI (e.g., 200)
PDF_PARSER_PAGE_DPI = 200

PDF_PARSER_MODEL_PATH = BASE_DIR / "docling_models"  # Docling 模型目录

# --- 阶段3：文本分块 (Chunking) 配置 ---
PDF_PARSER_CHUNKING = {
    # 是否在解析表格时清理多余的空格
    "CLEAN_TABLE_WHITESPACE": False,
    # 动态块大小的参数
    "MIN_CHUNK_SIZE": 100,
    "MAX_CHUNK_SIZE": 500,
    # 动态块大小乘数 (n * max_block_size)
    "N_FACTOR": 1.5,
    # 滑动窗口大小 (例如 2 表示 [chunk-1, chunk, chunk+1] 总共3个)
    "WINDOW_SIZE": 2
}

# --- 阶段4：型号抽取 (Model Extraction) 配置 ---
PDF_PARSER_MODEL_EXTRACTION = {
    # 用于并发调用 LLM API 的线程池工作线程数
    "MAX_WORKERS": 5,
    # LLM API 调用失败的最大重试次数
    "MAX_RETRIES": 3,
    # LLM 调用的 temperature
    "TEMPERATURE": 0.0,
}

# --- 阶段6：参数提取 (Param Extraction) 配置 ---
# (注意：LLM 配置复用 DEFAULT_LLM)
PDF_PARSER_PARAM_EXTRACTION = {
    # 失败重试次数 (与阶段4分开)
    "MAX_RETRIES": 3,
    # API 调用之间的礼貌性延迟（秒），避免速率限制
    "API_CALL_DELAY": 1.0,
    # LLM 调用的 temperature
    "TEMPERATURE": 0.2,
    # LLM 调用的 max_tokens
    "MAX_TOKENS": 8000
}

# --- 阶段7：参数融合 (Param Fusion) 配置 ---
PDF_PARSER_PARAM_FUSION = {
    # LLM API 调用超时（秒）
    "API_TIMEOUT_SECONDS": 60,
    # LLM API 调用失败的最大重试次数
    "MAX_API_RETRIES": 3,
    # 重试前的等待时间（秒）
    "RETRY_DELAY_SECONDS": 5,
}